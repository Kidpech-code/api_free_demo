# Git
.git
.gitignore

# CI / local tooling
.github
.vscode
.idea

# Docker files themselves
Dockerfile
.dockerignore

# Go test & profiling artifacts
**/*_test.go
*.test
*.prof

# UI mockup (not needed in container)
ui-mockup/

# Environment files (never bake secrets into image)
.env
.env.*
!.env.example

# OS noise
.DS_Store
Thumbs.db

# Dependency vendor (we use go mod download instead)
vendor/

# Build outputs
bin/
